app = "life-os-bot"
primary_region = "sin"

[build]
  dockerfile = "Dockerfile"

[deploy]
  release_command = "./life-os-migrate up"
  release_command_timeout = "5m"
  strategy = "rolling"

[env]
  APP_TIMEZONE = "Asia/Ho_Chi_Minh"
  GOOGLE_CALENDAR_ID = "primary"
  HTTP_ADDR = ":8080"
  # Set GOOGLE_OAUTH_REDIRECT_URL and GOOGLE_CREDENTIALS_JSON as secrets for per-user calendars.
  # Set CALENDAR_TOKEN_ENCRYPTION_KEY before users connect calendars.
  MIGRATIONS_SOURCE = "file:///app/migrations"
  AUTONOMY_SCHEDULER_ENABLED = "true"
  AUTONOMY_DEFAULT_ENABLED = "false"

[http_service]
  internal_port = 8080
  processes = ["app"]
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1

[processes]
  app = "./life-os-bot"

[[vm]]
  memory = "512mb"
  cpu_kind = "shared"
  cpus = 1
